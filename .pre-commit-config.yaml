exclude: |
  (?x)^(
    \.venv/|
    build/|
    dist/|
    .*\.egg-info/|
    .*__pycache__/|
    node_modules/|
    tests/golden/|
    src/attune_rag/dashboard/templates/.*\.html|
    src/attune_rag/editor/template_schema\.json
  )

repos:
  - repo: https://github.com/psf/black
    rev: 24.10.0
    hooks:
      - id: black
        args: ['--line-length=100']
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.4
    hooks:
      - id: ruff
        args: ['--fix', '--exit-non-zero-on-fix']
      - id: ruff
        name: ruff-bare-exception-check
        args: ['--select=BLE', '--no-fix']
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.5.0
    hooks:
      - id: detect-secrets
        # perf-thresholds.json is GENERATED (perf.yml lock-baseline) and
        # embeds the measured commit's 40-char SHA — a guaranteed
        # hex-high-entropy false positive that ROTATES on every re-lock,
        # so a baseline pin goes stale each time. Exclude the file.
        args: ['--baseline', '.secrets.baseline',
               '--exclude-files', 'docs/specs/downstream-validation/perf-thresholds\.json']
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-toml
      - id: check-json
      - id: check-added-large-files
        args: ['--maxkb=1200']
      - id: check-merge-conflict
      - id: mixed-line-ending
        args: ['--fix=lf']
