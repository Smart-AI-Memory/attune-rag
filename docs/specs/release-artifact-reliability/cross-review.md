# Claude subscription cross-review — Task 3

**Advisory only.** Actual installed `attune.roundtable.review.run_review` result; six files sent, zero omitted, 71,260 diff characters (250,000 cap). Board thread: `review-main-20260910-0222`; posting status: `posted`. Claude subscription authentication was verified; API credentials were removed and fallback forbidden. This is authentication-route evidence, not an invoice or overage receipt.

The real Git index, HEAD, and six reviewed working files were unchanged by the runner. Original findings below retain the reviewer’s claims and reviewed-snapshot line numbers. Claims are not accepted until checked against the full implementation; triage follows separately.

- **F1 [medium] [src/attune_rag/benchmark.py:1108](../../../src/attune_rag/benchmark.py#L1108):** `--compare-thinking` combined with `--native-citations` now sets `use_native_citations=args.native_citations` on the secondary pass while storing/labelling it as `faithfulness_thinking_on` / "thinking on"; the old code forced native off for pass B, so the A/B comparison silently varies two independent variables.
- **F2 [medium] [scripts/check_thresholds.py:205](../../../scripts/check_thresholds.py#L205):** `_quality_value` is applied to every entry in `thresholds.metrics`, including skipped and unmeasured ones, so any future non-[0,1] threshold (latency, counts, token budgets) makes the gate a permanent exit 2 rather than a metric the checker ignores.
- **F3 [medium] [src/attune_rag/benchmark.py:894](../../../src/attune_rag/benchmark.py#L894):** the new catch-all prints only `type(exc).__name__`, discarding the message; the actionable `ValueError` text from `_load_queries(args.negatives/args.extended/args.corpus_queries)` and from the `--min-precision`/`--min-faithfulness`/`--k` validators is lost, leaving CI with only `error: local_error:ValueError`.
- **F4 [low] [src/attune_rag/benchmark.py:996](../../../src/attune_rag/benchmark.py#L996):** moving the precision gate ahead of the negatives/extended/generalization passes means a retrieval regression now produces none of the advisory diagnostics that previously accompanied the failure, exactly when triage needs them.
- **F5 [low] [.github/workflows/benchmark.yml:196](../../../.github/workflows/benchmark.yml#L196):** the outcome validator hardcodes `"faithfulness_legacy" in report` for `completed`, so a `--compare-thinking` run (which writes `faithfulness_thinking_off`) would be rejected as invalid evidence; `extract_metrics` has the same single-key assumption.
- **F6 [low] [.github/workflows/benchmark.yml:301](../../../.github/workflows/benchmark.yml#L301):** `always() && rc != '0' && rc != '1'` also fires on job cancellation and whenever an earlier step fails and `check` is skipped (empty `rc`), attributing unrelated failures to "validation error".
- **F7 [low] [.github/workflows/benchmark.yml:218](../../../.github/workflows/benchmark.yml#L218):** RC precedence maps `BENCH_RC=2` plus `FAITH_RC=1` to `RC=1`, reporting a local/harness failure as a measured quality regression (covered and locked in by the `completed-faithfulness-before-error` case).
- **F8 [low] [scripts/check_thresholds.py:74](../../../scripts/check_thresholds.py#L74):** `math.isfinite(value)` is unreachable — `0 <= value <= 1` already rejects NaN and ±inf — so the ordering comment documents dead code.
- **F9 [low] [src/attune_rag/benchmark.py:977](../../../src/attune_rag/benchmark.py#L977):** `--calibrate --with-faithfulness` returns 0 while leaving `outcomes.faithfulness == "pending"`, i.e. a success exit paired with an unresolved outcome; only the workflow validator catches this, not the CLI.
- **F10 [low] [src/attune_rag/benchmark.py:975](../../../src/attune_rag/benchmark.py#L975):** `payload["calibration"] = cal` is not pre-checked with `json.dumps(..., allow_nan=False)` the way `negatives` is, so one non-finite calibration value fails the entire receipt write instead of that section.
- **F11 [low] [src/attune_rag/benchmark.py:642](../../../src/attune_rag/benchmark.py#L642):** `_save_progress` now serialises and rewrites the dump up to three times per run and prints "Wrote per-query JSON" to stderr each time; the final write in `main` duplicates the checkpoint written by `_run_faithfulness_passes`.
- **F12 [low] [src/attune_rag/benchmark.py:127](../../../src/attune_rag/benchmark.py#L127):** the generator variable `path` in the `expected_in_top_3` check shadows the `_load_queries` parameter of the same name used two lines above in the error message; correct by genexp scoping but easy to misread and to break in a later edit.
- **F13 [low] [tests/unit/test_benchmark.py:812](../../../tests/unit/test_benchmark.py#L812):** the `bad-options` case assumes a bare `--compare-thinking` is rejected, but the only guard visible in the diff is `if args.compare_thinking and args.thinking`; confirm an explicit "requires --with-faithfulness" check exists or the case asserts the wrong exit path.
- **F14 [low] [tests/unit/test_quality_workflow_outcomes.py:76](../../../tests/unit/test_quality_workflow_outcomes.py#L76):** `_dump()` pins the real `tests/golden/queries.yaml` and repo `thresholds.json`, so every workflow-replay case fails during any PR that legitimately re-measures the baseline sha, and the 20s subprocess timeout at line 113 is tight given each case spawns four-plus real interpreters.

## Final disposition — 2026-09-09 America/New_York

All 14 claims were checked against the full implementation. The raw result, original claims above, and runner postconditions remain unchanged; the table records subsequent triage and corrections.

| Findings | Disposition | Evidence |
|---|---|---|
| F3 | Real; corrected | Controlled local validation diagnostics preserve actionable fixed messages while arbitrary exceptions remain class/stage-only: `benchmark.py:50,119–144,656–670,909–918`. Public CLI regression tests: `test_benchmark.py:864–1004`. |
| F6 | Real; corrected | Empty checker output now says required validation did not complete; nonempty invalid codes retain validation guidance. The fail-closed guard is unchanged: `benchmark.yml:302–311`; actual YAML shell tests at `test_quality_workflow_outcomes.py:322–340`. |
| F9 | Real; corrected | Calibration-only success explicitly skips both retrieval and faithfulness with `reason=calibration_only`: `benchmark.py:994–1007`; real calibration regression test at `test_benchmark.py:1007–1039`. |
| F1, F2, F8, F10, F12, F13, F14 | Noise as current defect claims | Existing CLI guards, the active ratio-only checker, reachable finite checks, strict final serialization, generator scoping, and current-file SHA validation disprove the stated problems. No observed timeout defect establishes F14. |
| F4, F5, F7, F11 | Intentional behavior | Early primary verdicts, default-pass gate scope, preserving measured regression precedence, and staged checkpoints match the accepted contract. Additional advisory diagnostics, alternate gate modes, and log deduplication remain optional later work. |

Full-code reasons and original local probes: [workflow/checker triage](receipts/task-3-cross-review-workflow-triage.md), [benchmark triage](receipts/task-3-cross-review-benchmark-triage.md). Those notes retain pre-correction line references and observations. [Independent follow-up](receipts/task-3-cross-review-independent.md) found no actionable defect in the three corrections and assigned no new score. The refreshed integrated suite reports **1,505 passed, 2 XFAIL, 78 XPASS, 93.03% coverage**; the rebuilt sdist/wheel and four artifact controls pass. [Bound follow-up receipt](receipts/task-3-cross-review-followup.json), [Task 5 receipt](receipts/task-5-review.json).

This closes the advisory review without changing thresholds, query locks, task acceptance, or later-phase scope. No additional model/provider calls were made during triage or local follow-up. Release 1.2.1 preparation is separately authorized by D13 and is not a published-release or remote-CI result.
