# Phase 1 scoping decisions

> **Status: Phase 1 complete locally; all five tasks accepted under authorized auto-run.**

**D1 — Program goal accepted (2026-09-09).** Patrick agreed with the recommended goal: the installed library preserves measured retrieval quality, returns current and correctly grounded results, and fulfills its documented contracts.

**D2 — Phase 1 scope selected (2026-09-09).** After choosing the full reliability program in separate milestones, Patrick clarified that this session should create a Phase 1 spec for packaging and CI. The remaining review findings will be used to scope Phase 2 or later specs, rather than being included in this implementation scope.

**D3 — Review is a source of goals (2026-09-09).** Patrick explicitly requested that the executive review supply goals for the specs. The [goal inventory](../RELIABILITY_GOALS.md) records that relationship. Candidate future goals are not implementation approvals.

**D4 — Code is the authority for current state (2026-09-09).** Patrick explicitly directed: “use the code as the ultimate authority of our current state.” Apply this to every phase: inspect the selected checkout and active configuration, treat the review as derived evidence and a source of goals, distinguish documented intent from implemented behavior, and correct or retire any finding contradicted by the code. Tests and reproductions support bounded observations; they do not replace inspection of the implementation.

**D5 — Phase 1 plan approved (2026-09-09).** Patrick stated “phase 1 is aproved” after receiving the five-task draft, its packaging/release/CI scope, and the task-boundary acknowledgment request. This approves the Phase 1 plan and acknowledges task-boundary chair-review receipt `30b62211ab53`. The canonical workspace accepted `acknowledge_gate` at revision 3, then `approve_plan` at revision 4, advancing workspace `spec-395632a111494410b7bfafad8f70c9d8` to revision 5, stage execution approval. All five tasks remain pending. This records plan approval; no execution-start choice, completed task, release, or publication is recorded.

**D6 — Execution authorized (2026-09-09).** Patrick answered “yes” to starting implementation of the approved five-task plan. The canonical workspace accepted `start_execution` at revision 5. Execution checks returned two mechanical PASS receipts and chair-review receipt `c2f779b681eb` for the same approved packaging/release/CI scope, with no changed paths or new findings. The existing scope acknowledgment in D5 and explicit execution confirmation apply to that unchanged work; the workspace accepted the bound acknowledgment at revision 7. This records the application of existing authorization, not a new user statement about a later receipt. Task 1 has started; no task has completed and no release is authorized.

**D7 — Review without Claude (2026-09-09).** Patrick requested the attune-release-check skill and clarified that review should be possible without Claude. Phase 1 will use Codex code-quality/security review and local tests/static checks instead of the Claude-backed automated review runner. This changes the review mechanism, not the acceptance criteria or scope, and does not authorize exporting code to Claude. Reviewer scores required by the task-receipt interface are labeled subjective judgments, separately from measured test/artifact results. The failed and rejected Claude attempts remain recorded as historical facts.

**D8 — Record workflow portability follow-up (2026-09-09).** Patrick asked for TODOs to check workflows on more than one model, given the current Claude dependency. The [workflow portability backlog](../WORKFLOW_PORTABILITY_TODO.md) records that work, including a non-Claude backend and shared contract checks. The likely owner is attune-ai; Phase 1's approved task scope is unchanged.

**D9 — Task 1 accepted; auto-run authorized (2026-09-09).** Patrick answered “y go” to accepting Task 1 and automatically running the remaining four Phase 1 tasks with Codex/local checks. The canonical workspace accepted `auto_run_remaining` at revision 10, recorded Task 1 complete, and returned persisted state with `auto_run=true`. Subsequent acceptable task results advance automatically; high-severity findings still require resolution. This does not authorize releases or the later-phase backlog.

**D10 — Advisory cross-review and billing clarification (2026-09-09).** Patrick requested `/cross-review` for debugging, then clarified that Claude participation is acceptable; his concern is separate API charges instead of subscription use. This supersedes an interpretation of D7 as a general objection to Claude. One advisory Claude review may use the skill's verified subscription route, with API credentials removed and no API fallback. The local task-review mechanism remains in place. The restricted child initially reported no login. A read-only check outside the sandbox then verified `authMethod=claude.ai`, `subscriptionType=max`, and no API-key source. Neither preflight invoked a model. Automatic approval review subsequently rejected transmitting the six-file diff because it required exact-payload authorization; no review request was sent by that rejected attempt. D11 records the subsequent authorization and invocation. Authentication availability and billing claims must be evidenced separately. The portability backlog now includes this boundary.

**Current:** All five tasks accepted; local Phase 1 implementation and refreshed verification are complete. D11’s advisory review is complete with all 14 claims triaged and F3/F6/F9 corrected. Later-phase goals remain unapproved. D13 separately authorizes release execution; 1.2.1 preparation is in progress, with no publication or remote CI success claimed by these receipts.

Machine verdicts live in [gate-receipts.jsonl](gate-receipts.jsonl), separately from these human decisions.


**D11 — Execute the requested subscription review (2026-09-09).** In response to the exact-payload question, Patrick supplied an explicit `run_review` command selecting the Claude seat and `claude_auth='subscription'`. The moderator verified that branch mode contained no changed files: Phase 1 is uncommitted. The command was therefore applied to the already identified six-file Task 3 diff using a scratch Git index/object directory, preserving the real index and HEAD. The installed runner’s subscription authentication checks, credential removal, disabled tools/integrations, and no-API-fallback behavior remain in effect. Automatic approval review accepted the new invocation; the runner started. This authorizes one advisory review, not API billing, publication, or a new implementation phase.

**D12 — Phase 1 task ladder complete (2026-09-09).** The canonical workspace accepted the passing Task 5 receipt under D9 auto-run and reached terminal revision 19 with tasks 1–5 completed. The embedded plan state was saved through `attune.spec.save_state`. Local tests, final artifacts, negative controls, and code-grounded documentation were independently reviewed. The separate Claude cross-review remains advisory and cannot retroactively substitute for those receipts.

**D13 — Separate release execution authorized (2026-09-09, America/New_York).** Patrick invoked `release-execute` after Phase 1 completion and advisory-review follow-up. The lead selected patch release 1.2.1 and began preparation in `/private/tmp/attune-rag-release-1.2.1`, a separate checkout based on verified current main `f00815d`; only that release checkout has the version bump. This is a new release-execution authorization, distinct from D5–D9’s Phase 1 approval. Phase 1 remains complete at canonical terminal revision 19, and later reliability goals remain unapproved. At this receipt closeout, release preparation is in progress; a published release, successful remote CI, and completed publication are not recorded.
