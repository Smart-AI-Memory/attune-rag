# Phase 1 evidence record

> **Status: Phase 1 complete locally; all five tasks accepted under authorized auto-run.**

**Primary authority:** The initial review examined implementation and active build/workflow configuration at `25d2cf00bbe4a71daa7e8894402153ed6465d238` / 1.2.0 on 2026-09-09 America/New_York. The E1–E6 table below records that pre-implementation snapshot. Current implemented behavior includes the local Phase 1 edits and the F3/F6/F9 corrections; the final receipts below bind the tested source and artifact hashes. The [executive review](../../reviews/library-review-2026-09-09.md) is the derived findings and goals record. Cached `origin/main` was `f00815d`, a documentation-only successor. Revalidate against the selected implementation base before changing code.

**Evidence rule:** Describe current behavior from code and configuration, and qualify runtime observations by the environment and paths tested. A passing test, README claim, archived spec, or review conclusion cannot override contradictory code. Record the mismatch, correct the current-state account, and distinguish any proposed behavior change from an existing capability. External provider facts require provider evidence; local code establishes which provider behavior the library attempts to use, not whether a deployed service is available.

| ID | Reproduced observation | Authoritative implementation |
|---|---|---|
| E1 | Both corpus JSON overrides absent from clean sdist and wheel; editor schema present | `pyproject.toml` package-data; `src/attune_rag/corpus/attune_help.py` resource loading |
| E2 | Same 40 queries and attune-help 0.13.0: source 40/40 top-1 and 40/40 top-3; wheel 32/40 and 39/40 | Golden query YAML, `_scoring.score_queries`, `RagPipeline` |
| E3 | Mocked faithfulness 0.50 → benchmark exit 1 with failing JSON → actual workflow shell exit 0, `inconclusive=true` | `benchmark.py` internal cutoff; `benchmark.yml` benchmark step and conditional threshold step |
| E4 | All required metrics set to NaN → threshold checker returns no failures and no validation errors | `scripts/check_thresholds.py:check` |
| E5 | Local performance-process failure explicitly becomes RC=0; only two CPU axes selected as blocking | `.github/workflows/perf.yml` measurement and delta steps |
| E6 | Release workflow builds sdist/wheel, uploads, and publishes without artifact behavior validation | `.github/workflows/publish.yml` |

**Independent build reproduction.** The lead exported `git archive HEAD` into a temporary directory, ran the declared build lifecycle with the locally available backend using `python -m build --no-isolation --skip-dependency-check`, inspected the wheel, and ran scoring twice in separate subprocesses with verified source/wheel import paths. Build isolation was disabled only to avoid dependency downloads during review; normal isolated release builds remain the production path. A separate review lane reproduced tracked-source → sdist → wheel with the same missing files and scores.

**Independent gate reproduction.** The faithfulness function was replaced with an `AsyncMock` returning 0.50. The real benchmark emitted its JSON and exit 1. The actual workflow shell was replayed with only the benchmark executable return and retry sleep stubbed. It returned 0 and skipped threshold evaluation. The emitted JSON was separately rejected by the real checker at the active 0.9698 threshold. No LLM request was made.

**Active baseline:** `docs/specs/release-quality-baseline/thresholds.json`: P@1 0.975; R@3 1.0; faithfulness 0.9698; query SHA `f47486df87c61e2391a4326f9ffe68659373f6837063cf70c5b990a639f2290c`. Do not replace it with earlier numbers quoted in README or archived baseline prose.

**Intended exception versus reviewed behavior:** `docs/specs/archive/release-quality-baseline/tasks.md`, M3.4, records an intended policy of one retry for transient API failure followed by explicit inconclusive/nonblocking status. The workflow at the reviewed baseline instead retried every full-mode nonzero result once and converts remaining failures to inconclusive success; retrieval-only failures become inconclusive without retry. That baseline code established the catch-all behavior; Task 3 subsequently implemented the narrower policy described in its acceptance record below. The archived spec is a compatibility/design input, not proof that the narrow classification exists today.

**Publication configuration versus remote enforcement:** `.github/workflows/publish.yml` configures OIDC permissions and `environment: pypi`. This establishes the workflow configuration, not current remote required-reviewer settings, which were not inspected. Phase 1 preserves that configuration without claiming or changing external approval enforcement.

**Suite context:** Source-mode run: 1,114 passed, 1 metadata mismatch failure, 2 xfailed, 78 xpassed; combined coverage 92.03%. The existing virtual environment reports attune-rag 0.9.0 while source and pyproject both declare 1.2.0. This is environment evidence, not a request to lower source version. Artifact validation must start clean.

**Limits:** This review did not download the published PyPI package, verify current remote branch protection/check runs, spend API tokens, benchmark real embedding models, or validate deployed consumers. These findings establish local code/build defects. They do not claim a production incident or a complete audit of every possible failure.

**Spec validation:** `read_spec()` parsed five ordered tasks, each with an objective and validation checks, and every declared modification target existed. Independent spec review clarified retrieval-only artifact thresholds, removed runtime corpus-loader changes, and retained the existing benchmark stdout consumer's compatibility checks. The task lifecycle runner returned two mechanical PASS receipts and one CHAIR_REQUIRED receipt. Its symbol check examined zero cited tokens in the overview; this is recorded explicitly rather than presented as validation of every task symbol.

The CLI initially could not append to its default ledger outside the workspace (`~/.attune/ops/gates/verdicts.jsonl`). The same supported `run_boundary()` API was then run with its explicit `ledger_file` parameter pointing to this spec's `gate-receipts.jsonl`; gate policy and planned paths were unchanged. No global ledger permission or configuration was changed.

## Task 1 implementation evidence — 2026-09-09

The source base remains `25d2cf0`; the tracked modification adds the two corpus JSON files to package data. New checker/tests are currently untracked working files. The checker snapshots current tracked contents, including tracked edits and deletions, and excludes untracked files. This preserves the release-input boundary; it does not claim the new development scripts were present in the original commit.

- [Build receipt](receipts/task-1-build.json): clean tracked snapshot → sdist → wheel using the default build lifecycle, with local `--no-build-isolation`. The prepared setuptools backend was used without dependency downloads. This is a local macOS/Python 3.11 check, not a claim of passing the remote matrix or an isolated-network build.
- [Final installed-artifact receipt](receipts/task-1-distribution.json): validates the same built bytes without rebuilding; all 45 package-source/required-resource members match their snapshot inputs. A fresh temporary virtual environment installs the wheel with pip, `--no-index`, and `--no-deps`. Both probes run in fresh `-I -S` processes against the same existing dependency directories, avoiding user-site, inherited PYTHONPATH, and editable `.pth` fallback.
- Both source and wheel use attune-help 0.13.0 and report version 1.2.0. All 40 queries return identical ordered paths and scores, with P@1 1.0 and R@3 1.0 against the unchanged query SHA and active thresholds. Recorded hits and aggregate metrics come from the same scorer pass. Faithfulness is explicitly skipped; the artifact probes make no provider calls.
- [Historical negative receipt](receipts/task-1-historical-negative.json): the original locally built `25d2cf0` distributions fail the new checker for a missing corpus override. The successful and historical artifacts are local builds, not downloaded PyPI releases.
- Final focused distribution tests: 65 passed. Final targeted distribution/corpus/golden run: 151 passed, 2 xfailed, 78 xpassed; advisory paraphrase statuses retain their existing policy. Ruff, Black, and git diff checks pass for the new script/tests. No full-suite or remote CI success is claimed for this task. The [JUnit report](receipts/task-1-tests.xml) records the targeted run.
- Independent code review identified incomplete-sdist validation and a report path that could overwrite an artifact. Both were corrected and covered by negative controls; follow-up review found no residual material issues in those changes.

Receipt import paths identify the temporary locations used during execution; those environments are removed after each check. Artifact files remain under `/tmp/attune-rag-phase1-task1-rebuilt/`. The sdist SHA-256 is `6caf41282012fd3df540139904e8b7ebf4281aed0778947b36ba255d2f9968cc`; wheel SHA-256 is `fa3bb05893250e572e4811078397f1edf6a4fad4b63d6246c66a2b21c2dc28ca`. These receipts validate Task 1 behavior; workflow integration and publication binding remain Task 2.

The spec orchestrator's automated quality review was invoked separately with its duplicate test run and optional simplify workflow disabled. Both Claude SDK review lanes reported `ENOTFOUND` reaching their API server. That run was stopped after the confirmed network failure. Automatic approval review rejected a network-enabled rerun because sending repository code and review context to Claude requires explicit authorization for that destination. The rerun did not execute. The [original attempt receipt](receipts/task-1-quality-gate.json) preserves those facts; its blocked status describes that historical route, not the replacement review below.

Patrick then explicitly selected review without Claude ([D7](decisions.md)). The [replacement review receipt](receipts/task-1-local-review.json) records independent Codex code-quality and security reviews, local tests, lint/format checks, and the [local security scan](receipts/task-1-security-scan.json). Review scores of 93 and 96 are subjective assessments, separately labeled from measured results; both exceed the existing 70-point review threshold. All 10 conservative medium path warnings were reviewed in context as trusted script locations, temporary paths, checked metadata origins, controlled internal probe arguments, or test assertions; there are no unresolved actionable findings.

The security review also found that inherited pip configuration could redirect installation. The checker now removes inherited `PIP_*` settings and disables pip configuration for build/install subprocesses. Bundled-pip parser tests prove both the original redirection and its prevention. A final actual installed-wheel run with poisoned `PIP_TARGET`, `PIP_PREFIX`, and `PIP_USER` passed; neither unwanted destination was created. No external model calls were made for this replacement review.

Task 1's passing local result was published at revision 10. Patrick then accepted Task 1 and authorized auto-run of the remaining tasks (D9). The separately requested [release check](receipts/task-1-release-check.md) stopped at its first failure: version 1.2.0 already exists on PyPI. At that Task 1 checkpoint, no release was authorized or performed. D13 records the later, separate release-execution request.


## Tasks 2–4 accepted results — 2026-09-09

**Task 2 — Artifact publication binding.** The test workflow now builds and validates a distribution in a separate Ubuntu/Python 3.11 job. The publish workflow uses the same checker, uploads only successfully validated artifacts, and carries the report checksum as a build-job output. Immediately before the unchanged OIDC publisher, it verifies the report checksum, exact two-file artifact set, and both artifact hashes. It does not rebuild. Eleven tests replay the actual YAML scripts, and the [transfer receipt](receipts/task-2-transfer.json) records a transfer of the actual Task 1 distributions plus a rejected byte alteration. [Independent review](receipts/task-2-review.json) reports quality 94/security 97 (subjective judgments), with no unresolved findings. The configured test matrix and `pypi` environment remain intact; remote CI, environment reviewers, OIDC authentication, and publication were not exercised.

**Task 3 — Quality outcome propagation.** The benchmark checkpoints validated retrieval before cutoffs/providers and checkpoints primary faithfulness before comparisons. Its JSON adds `outcomes`; exit 0 means pass, 1 regression, 2 invalid/local failure, and 3 classified transient primary provider failure. The workflow retries at most once and only when exit 3 is corroborated by unavailable primary faithfulness and a passing current retrieval report. It always checks required retrieval and validates completed primary faithfulness against the locked thresholds. Missing/corrupt/stale reports, nonfinite or out-of-domain values, invalid types, SHA failures, local errors, and measured regressions cannot become successful validation. The existing stdout consumer remains compatible. The [254-test receipt](receipts/task-3-tests.xml) and [local review](receipts/task-3-review.json) establish this with stubbed providers and actual shell/checker execution; no live faithfulness inference was used. Thresholds and query lock are unchanged.

**Task 4 — Required CPU evidence.** Both selected CPU metrics require valid current and baseline data. The measure step clears stale outputs; unsuccessful measurement outcomes and invalid selected inputs fail validation. Validation errors replace stale passing comments. Genuine regressions in the two selected CPU axes fail, while all six other valid metric regressions remain advisory. The [158-test receipt](receipts/task-4-tests.xml) executes the formatter and actual workflow shell; the [review](receipts/task-4-review.json) found no unresolved findings. Baselines, threshold values, measurement/aggregation scripts, and variance methodology are unchanged. No live performance baseline was reset or remote CI run claimed.

**Advisory cross-review.** Patrick requested one cross-model review and clarified that Claude is acceptable through subscription use, without separate API spend. The exact six-file Task 3 diff was prepared with a scratch Git index/object directory; all six files fit the manifest. A read-only preflight outside the sandbox verified a saved Claude Max login without an API-key source. Automatic approval review rejected the first transmission attempt pending exact-payload authorization; no review was sent by that attempt. Patrick subsequently supplied the subscription command, and the new invocation was accepted (D11). The runner completed with 14 claims. Full-code triage accepted F3/F6/F9 as real and corrected all three; F1/F2/F8/F10/F12/F13/F14 are unsupported as current defects, while F4/F5/F7/F11 describe intentional behavior. [Original claims and final dispositions](cross-review.md), [status receipt](receipts/task-3-cross-review.json), and [immutable runner postconditions](receipts/task-3-cross-review-postconditions.json) preserve the record. The runner itself left the real index, HEAD, and six working files unchanged; subsequent authorized corrections changed four files, whose hashes are recorded in the [follow-up receipt](receipts/task-3-cross-review-followup.json). This advisory review does not substitute for task acceptance or behavioral validation.


## Task 5 integrated verification — 2026-09-09

**Final serial suite after advisory corrections:** 1,505 passed, 2 xfailed, 78 xpassed in 55.79 seconds (console; JUnit duration 55.781 seconds); combined branch/statement coverage 93.03% (93.02918017600742% in the receipt), exceeding the existing 77% floor. [JUnit](receipts/task-5-tests.xml), [coverage](receipts/task-5-coverage.json). The 2 XFAIL/78 XPASS cases retain the existing informational paraphrase policy; they are not presented as ordinary passes. This suite includes the resource/parity negative controls and actual quality/performance/publication workflow shell tests. Provider results are stubbed; no live provider inference or embedding-model download was used.

**Environment correction.** A fresh Python 3.11.14 virtual environment reused the already installed, same-Python dependency set without downloads. The first setup exposed dependencies only through a `.pth` entry, which the intentionally isolated `-I -S` probes do not process; two probes correctly failed on missing dependencies. The checkout also contained ignored generated `src/attune_rag.egg-info` at version 0.9.0, shadowing freshly installed 1.2.0 metadata. The [initial failing run](receipts/task-5-initial-environment-tests.xml) is preserved. Dependencies were then made directly visible through links to existing dependency entries, inherited editable hooks were excluded, and the stale ignored metadata was preserved at `/private/tmp/attune-rag-phase1-stale-0.9.0-egg-info`. Fresh editable installation supplied the correct metadata. All 66 metadata/distribution tests passed before the final serial suite. The project's existing `.venv` was also reinstalled locally with `--no-index --no-deps --no-build-isolation`; its metadata test passes. Production version constants and assertions were unchanged.

**Final distribution.** [Receipt](receipts/task-5-distribution.json): current tracked snapshot → sdist → wheel from sdist, using the prepared local backend (`--no-build-isolation`). Both archives contain the same 45 package-source/required-resource members as the snapshot. Separate isolated source and fresh installed-wheel probes use attune-help 0.13.0 and identical dependency versions. All 40 queries return identical ordered paths and scores; P@1 and R@3 are both 1.0, meeting the unchanged 0.975/1.0 thresholds and query SHA. Faithfulness is explicitly skipped. Temporary probe environments are removed after checking; receipt import paths record historical execution locations.

Final artifacts under `/private/tmp/attune-rag-phase1-post-review-dist/`:

- `attune_rag-1.2.0.tar.gz` — SHA-256 `7f0eaf5ff5dfa85b13997bce460bef18a74594fc30777bcc0110af2054590153`.
- `attune_rag-1.2.0-py3-none-any.whl` — SHA-256 `d051bfc33a45fec8550e398bfb819afe288a18ba66f261e0b6269d2203a499e9`.

**Final artifact negative controls.** Starting from the final wheel, remove each corpus JSON override separately: both fail with a named missing-resource error and exit 1. Copy the actual final artifacts and their receipt through the literal publication integrity/verification scripts: accepted. Append bytes to the copied wheel after validation: rejected for checksum mismatch. Original artifacts remain unchanged. The successful transfer is bound to distribution report SHA-256 `4f5ad65c377a0cfa2f08a2b5eb4763349053cf8d77cb211261e1093d39b64296`. [Receipt](receipts/task-5-negative-controls.json). No upload or publication occurred in these controls.

**Static checks and claims.** Ruff passes across `src/`, `tests/`, and the three modified/new checker scripts; Black passes for all eleven changed Python files; `git diff --check` passes. README values reference active JSON thresholds, accurately distinguish selected faithfulness and the two blocking CPU axes, and label these changes Unreleased. Tests documentation distinguishes the hard 40-query gate from advisory paraphrase cases. Independent documentation review found no actionable mismatch; code/configuration and query counts/hashes were checked directly. The active quality/performance threshold files and locked query set are unchanged from HEAD.

**Limits and retained work.** All validation was local macOS/Python 3.11; the configured Linux/macOS/Windows and Python 3.10–3.13 matrix was not run remotely. Build isolation was disabled for the local prepared-backend run; CI uses the normal isolated build. No live faithfulness/embedding benchmark, remote protection check, OIDC publication, version bump, commit, or push is claimed. Fresh builds contain the tracked working-tree inputs; currently untracked development scripts, tests, and spec documents must be included in a later commit before CI can consume them. The advisory Claude review and local corrective follow-up are complete. The independent follow-up found no actionable defect within F3/F6/F9 and assigned no numerical score; its source-review conclusion is distinct from the lead’s integrated test and artifact results. D13 separately authorizes 1.2.1 release execution, now in preparation in another checkout; these 1.2.0 local validation artifacts are not evidence that 1.2.1 has been published or passed remote CI. Broader review goals and the model/billing portability backlog remain separate future scopes.

**Preserved history and refreshed review.** The earlier 1,480-pass / 92.82% run, artifacts, four negative controls, and subjective 96-point review remain unchanged in `receipts/task-5-pre-cross-review-{tests.xml,coverage.json,distribution.json,negative-controls.json,review.json}`. The current [Task 5 review](receipts/task-5-review.json) refers to the refreshed results above and the [independent correction review](receipts/task-3-cross-review-independent.md), which inspected the four corrected files and their tests without rerunning the suite or assigning a new score. Current source snapshot SHA-256 is `1c70264caab94acf5367fe1a93db088033f7f3c8ddd3c43d1666080b5a200609`. File and receipt hashes in the [follow-up receipt](receipts/task-3-cross-review-followup.json) distinguish original claims, later corrections, and measured verification.
