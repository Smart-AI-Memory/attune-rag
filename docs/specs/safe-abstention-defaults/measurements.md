# safe-abstention-defaults — measured distributions

Generated by `scripts/measure_abstention_distributions.py`
(PYTHONPATH=src, keyword-only, offline). Regenerate with the
`--out` flag; do not hand-edit.

Set sizes: help_legit n=40, help_neg n=12, cb_legit n=11, cb_neg n=12.
NB: at n=11 the 95% legit bar allows zero drops
(10/11 = 90.9%) — the corpus_b bar is effectively 100%.

## M1 — score distributions (design.md §2, re-derived)

| statistic (median) | help legit | help neg | corpus_b legit | corpus_b neg |
|---|---:|---:|---:|---:|
| raw top-1 | 13.50 | 3.38 | 6.00 | 0.00 |
| top-1 / query-token | 3.79 | 0.58 | 1.67 | 0.00 |
| top-1 − top-2 gap | 2.62 | 0.68 | 4.00 | 0.00 |

## M1 — bundled-corpus calibration (re-derived)

Recommended bundled `min_score` = **5** (legit kept 98%, false-answer rate 8%, target legit-kept >= 95%).

### R1-strict view (gate set must hold 100%)

Max `min_score` keeping **100%** of the gate set = **3** (negatives abstained 41.7%).
Gate queries the recommended T=5 would drop (alias-remediation candidates):
- `SAST scan my repository` (top-1 score 3.75)

## M2 — C3 relative-heuristic sweep (decides Q3)

Legit-recall bar: >= 95% kept on BOTH corpora.

### per-token (top1/n_tokens >= τ)
- **FAILS** — best eligible threshold (0.0) abstains only 0.0% of negatives (min across corpora); the legit-kept-eligible range provides no useful separation.

### gap (top1 − top2 >= δ)
- **FAILS** — best eligible threshold (0.0) abstains only 0.0% of negatives (min across corpora); the legit-kept-eligible range provides no useful separation.

### combination (top1 >= 5 or per-token >= τ)
- **FAILS** — best eligible threshold (0.0) abstains only 0.0% of negatives (min across corpora); the legit-kept-eligible range provides no useful separation.

Single-token legit queries in help_legit: 0
Single-token legit queries in cb_legit: 0
