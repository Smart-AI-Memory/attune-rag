# Schedule recurring jobs

Use a cron expression to run a job on a repeating cadence, e.g.
nightly or hourly. The scheduler computes the next fire time after
each run. Recurring schedules are registered at startup and survive
restarts.
