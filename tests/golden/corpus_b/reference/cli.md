# CLI reference

Commands: `enqueue` adds a job, `worker` starts a processing loop,
`status` prints queue depth and in-flight counts, `requeue` moves a
dead-lettered job back to the queue, `purge` clears completed jobs.
