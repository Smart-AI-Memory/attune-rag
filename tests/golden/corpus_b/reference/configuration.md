# Configuration reference

Settings: `store_backend` (sqlite|redis), `max_attempts`,
`backoff_base_seconds`, `backoff_max_seconds`, `worker_concurrency`,
and `dead_letter_ttl_days`. Values load from a TOML file or matching
environment variables.
