# Client options

Constructor settings: `base_url`, `token`, `timeout_seconds`,
`max_retries`, `backoff_factor`, and `user_agent`. Options may also be
read from environment variables of the same name.
